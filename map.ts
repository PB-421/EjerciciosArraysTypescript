// teoria

//Transforma un array en otro, tienen que ser del mismo tamaño

/*

const arr = [1,2,3,4,5];

function transf(a: number):number {
    return a * 2;
}

const arr2 = arr.map(transf) // como el foreach, recibe una funcion, vale [2,4,6,8,10]
arr2.forEach((a:number) =>
    console.log(a));

console.log("Pasamos a la persona");

type Person = {
    name: string,
    age: number
}

const arrPer:Person[] = [
    {name: "Pablo", age:20},
    {name: "Alex", age: 19}
]

const arrNom:string[] = arrPer.map((Pers:Person):string => { //Tambien puede recibir el indice como el foreach
    return Pers.name; 
})

arrNom.forEach((a:string) =>
    console.log(a));

*/